{
  "name": "simple-factory",
  "version": "1.0.1",
  "description": "One factory method to rule them all.",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha",
    "debug": "node-debug node_modules/.bin/_mocha",
    "coverage": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report lcovonly -- -R spec",
    "coveralls": "npm run coverage && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "keywords": [
    "simple",
    "factory",
    "simple-factory",
    "classes",
    "mapper"
  ],
  "author": "Brian Mullan <bmullan91@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "mocha": "~2.0.1",
    "chai": "~1.10.0",
    "istanbul": "~0.3.2",
    "coveralls": "~2.11.2",
    "mocha-lcov-reporter": "0.0.1"
  }
}
